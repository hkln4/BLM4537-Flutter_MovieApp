enum RequestStatus { loading, loaded, error }

enum GetAllRequestStatus { loading, loaded, error, fetchMoreError }

enum WatchlistRequestStatus { empty, loading, loaded, error }

enum BookmarkStatus { none, added, removed, exists }

enum FavoritesRequestStatus { empty, loading, loaded, error }

enum FavoriteStatus { none, added, removed, exists }

enum ReviewsRequestStatus { empty, loading, loaded, error }

enum ReviewActionStatus { none, added, updated, deleted }
